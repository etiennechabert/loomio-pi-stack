# Cloudflare Email Worker Configuration
# Copy this file to .env.email-worker and configure with your values

# =============================================================================
# WEBHOOK URL (Required)
# =============================================================================
# The URL where incoming emails will be forwarded to
# For security, use an obscure path with a random token
# Generate a secure path:
#   echo "/email_processor/$(openssl rand -hex 32)"
#
# Example: https://loomio.example.com/email_processor/a1b2c3d4e5f6g7h8...
WEBHOOK_URL=https://loomio.example.com/email_processor/GENERATE_RANDOM_PATH

# =============================================================================
# EMAIL PROCESSOR TOKEN (Required for Security)
# =============================================================================
# Shared secret to authenticate requests from the Email Worker to Loomio
# Generate a secure random token:
#   openssl rand -hex 32
#
# IMPORTANT: This must match the EMAIL_PROCESSOR_TOKEN in your Loomio .env file!
EMAIL_PROCESSOR_TOKEN=GENERATE_WITH_OPENSSL_RAND_HEX_32

# =============================================================================
# FALLBACK EMAIL (Optional)
# =============================================================================
# If set, emails will also be forwarded to this address as a backup
# Useful for debugging or ensuring no emails are lost
# FALLBACK_EMAIL=admin@example.com
